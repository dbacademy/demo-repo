name: Delete Br on PR closed

on:
  pull_request:
    types:
      - opened

jobs:
  if_merged:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
    - run: |
        echo The PR was merged
        
